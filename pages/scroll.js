import PlaceholderView from '@/components/Selfware/PlaceholderView';

export default function ScrollPage() {
  return (
    <PlaceholderView
      title="Scroll"
      subtitle="Living document layer"
      description="Scroll is reserved for long-form records, evolving entries, and durable context that needs more room than a note."
    />
  );
}
